#include <stdio.h>
#include <stdlib.h>

int main() {
//While print double in printf, it doesn't matter using l or lf but we need to use lf in scanf 
//It's logical using double as lf 
//There aren't necessary & put in scanf in character array, if we put, there aren't problem too.

	return 0;
}
